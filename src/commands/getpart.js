module.exports = {
    name: "getpart",
    description: "gets information about a waste of space part",
    cooldown: 1,
    permissions: [],
    options: [],

    run: async(client, interaction) => {
        interaction.reply({content: "implementation pending"})
    }
}